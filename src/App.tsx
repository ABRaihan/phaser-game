import { PhaserGame } from "./game/PhaserGame";

function App() {
    // The sprite can only be moved in the MainMenu Scene

    return (
        <div id="app">
            <PhaserGame />
        </div>
    );
}

export default App;

